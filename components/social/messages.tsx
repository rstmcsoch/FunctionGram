"use client";
import {useState,useEffect,useRef,useCallback} from 'react';
import {Send,Search,SquarePen,ArrowLeft,Bookmark,Smile} from 'lucide-react';
import {Avatar,Empty,IconButton,Busy,request,timeAgo} from './common';
import {toast} from 'sonner';
import type {Person,Message} from '@/lib/types';
export function Messages({me,people,initialRecipient,onProfile}:{me:Person;people:Person[];initialRecipient:string|null;onProfile:(id:string)=>void}){
 const [recipient,setRecipient]=useState(initialRecipient||me.id),[query,setQuery]=useState(''),[body,setBody]=useState(''),[messages,setMessages]=useState<Message[]>([]),[inbox,setInbox]=useState<Message[]>([]),[busy,setBusy]=useState(false),[loading,setLoading]=useState(true),[mobileChat,setMobileChat]=useState(!!initialRecipient),[error,setError]=useState('');const bottom=useRef<HTMLDivElement>(null),input=useRef<HTMLInputElement>(null),queryInput=useRef<HTMLInputElement>(null),sequence=useRef(0);const person=people.find(p=>p.id===recipient)||me;
 const load=useCallback(()=>{const version=++sequence.current;return Promise.all([request<Message[]>('/api/social?messages='+encodeURIComponent(recipient)),request<Message[]>('/api/social?inbox=1')]).then(async([items,all])=>{if(version!==sequence.current)return;setMessages(items);setInbox(all);setError('');await request('/api/social',{action:'read_messages',id:recipient});}).catch(e=>{if(version===sequence.current)setError((e as Error).message);}).finally(()=>{if(version===sequence.current)setLoading(false);});},[recipient]);
 useEffect(()=>{let active=true;const invalidate=()=>{sequence.current++;};void load();const timer=setInterval(()=>{if(active&&document.visibilityState==='visible')void load();},5000);return()=>{active=false;invalidate();clearInterval(timer);};},[load]);
 useEffect(()=>{bottom.current?.scrollIntoView({behavior:'smooth',block:'nearest'});},[messages.length]);
 const send=async()=>{if(!body.trim()||busy)return;setBusy(true);try{await request('/api/social',{action:'message',id:recipient,body});setBody('');await load();}catch(e){toast.error((e as Error).message);}finally{setBusy(false);}};
 const contacts=[me,...people.filter(p=>p.id!==me.id&&!p.is_demo)].filter(p=>(p.name+' '+p.username).toLowerCase().includes(query.toLowerCase()));
 return <div className={'messages-layout '+(mobileChat?'show-chat':'')}><aside className="conversation-list"><header><h1>Messages</h1><IconButton label="Find someone to message" onClick={()=>queryInput.current?.focus()}><SquarePen size={22}/></IconButton></header><label className="search-field"><Search size={18}/><input ref={queryInput} placeholder="Search people" aria-label="Search conversations" value={query} onChange={e=>setQuery(e.target.value)}/></label><h3>Your conversations</h3>{contacts.map(p=>{const last=inbox.find(m=>p.id===me.id?m.sender_id===me.id&&m.recipient_id===me.id:m.sender_id===p.id||m.recipient_id===p.id);return <button key={p.id} className={'conversation '+(recipient===p.id?'selected':'')} onClick={()=>{if(recipient!==p.id){sequence.current++;setRecipient(p.id);setBody('');setMessages([]);setLoading(true);setError('');}setMobileChat(true);}}><Avatar person={p} size={48}/><span><strong>{p.id===me.id?'Saved messages':p.username}</strong><small>{last?last.body:p.id===me.id?'Notes, links, and little reminders':'Start a conversation'}</small></span>{last&&<time>{timeAgo(last.created_at)}</time>}</button>;})}{!contacts.length&&<p className="no-results">No members found.</p>}<p className="messages-note">Messages are available between real members. Sample profiles don’t receive messages.</p></aside><section className="chat-panel"><header><IconButton className="chat-back" label="Back to conversations" onClick={()=>setMobileChat(false)}><ArrowLeft/></IconButton><Avatar person={person} size={40}/><button onClick={()=>onProfile(person.id)}><strong>{person.id===me.id?'Saved messages':person.username}</strong><span>{person.id===me.id?'Only you can see these messages':person.name}</span></button></header><div className="chat-content">{loading?<div className="loading-row"><Busy/></div>:messages.length?messages.map(m=><div key={m.id} className={'message-row '+(m.sender_id===me.id?'outgoing':'incoming')}><p>{m.body.split(/(https?:\/\/[^\s]+)/g).map((text,i)=>/^https?:\/\//.test(text)?<a key={i} href={text} target="_blank" rel="noreferrer" className="message-link">{text}</a>:text)}</p><time title={new Date(m.created_at).toLocaleString()}>{new Date(m.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time></div>):<Empty icon={person.id===me.id?<Bookmark/>:<Send/>} heading={person.id===me.id?'A little space for yourself':'Say hello'} body={person.id===me.id?'Save a thought, a link, or a reminder. It’ll be here when you need it.':'Start your conversation with '+person.name+'.'}/>}{error&&<div className="form-error" role="alert">{error}<button onClick={()=>void load()} className="text-action">Retry</button></div>}<div ref={bottom}/></div><form className="message-compose" onSubmit={e=>{e.preventDefault();void send();}}><IconButton label="Add a smile" onClick={()=>{setBody(v=>v+' 😊');input.current?.focus();}}><Smile size={22}/></IconButton><input ref={input} aria-label="Write a message" placeholder="Message…" value={body} maxLength={2000} onChange={e=>setBody(e.target.value)}/><button aria-label="Send message" className="message-send" disabled={!body.trim()||busy}>{busy?<Busy/>:<Send size={20}/>}</button></form></section></div>;
}
